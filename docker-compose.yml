version: '3.8'
services:
  db:
    image: postgres:15
    container_name: postgres_db_projeto
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: senha_faculdade
      POSTGRES_DB: db_projeto
    ports:
      - "5432:5432"